

void drawA()
{
  unsigned long t = millis();
  for (; millis() - t < 1000;) {
    blinkSegment(segAPin);
    blinkSegment(segBPin);
    blinkSegment(segCPin);
    blinkSegment(segEPin);
    blinkSegment(segFPin);
    blinkSegment(segGPin);
  }
}

void drawB()
{
  unsigned long t = millis();
  for (; millis() - t < 1000;) {
    blinkSegment(segCPin);
    blinkSegment(segDPin);
    blinkSegment(segEPin);
    blinkSegment(segFPin);
    blinkSegment(segGPin);
  }
}

void drawC()
{
  unsigned long t = millis();
  for (; millis() - t < 1000;) {
    blinkSegment(segDPin);
    blinkSegment(segEPin);
    blinkSegment(segGPin);
  }
}

void drawD()
{
  unsigned long t = millis();
  for (; millis() - t < 1000;) {
    blinkSegment(segBPin);
    blinkSegment(segCPin);
    blinkSegment(segDPin);
    blinkSegment(segEPin);
    blinkSegment(segGPin);
  }
}

void drawE()
{
  unsigned long t = millis();
  for (; millis() - t < 1000;) {
    blinkSegment(segAPin);
    blinkSegment(segDPin);
    blinkSegment(segEPin);
    blinkSegment(segFPin);
    blinkSegment(segGPin);
  }
}

void drawF()
{
  unsigned long t = millis();
  for (; millis() - t < 1000;) {
    blinkSegment(segAPin);
    blinkSegment(segEPin);
    blinkSegment(segFPin);
    blinkSegment(segGPin);
  }
}

void drawH()
{
  unsigned long t = millis();
  for (; millis() - t < 1000;) {
    blinkSegment(segCPin);
    blinkSegment(segEPin);
    blinkSegment(segFPin);
    blinkSegment(segGPin);
  }
}

void drawI()
{
  unsigned long t = millis();
  for (; millis() - t < 1000;) {
    blinkSegment(segCPin);
  }
}

void drawJ()
{
  unsigned long t = millis();
  for (; millis() - t < 1000;) {
    blinkSegment(segBPin);
    blinkSegment(segCPin);
    blinkSegment(segDPin);
  }
}

void drawL()
{
  unsigned long t = millis();
  for (; millis() - t < 1000;) {
    blinkSegment(segDPin);
    blinkSegment(segEPin);
    blinkSegment(segFPin);
  }
}

void drawN()
{
  unsigned long t = millis();
  for (; millis() - t < 1000;) {
    blinkSegment(segCPin);
    blinkSegment(segEPin);
    blinkSegment(segGPin);
  }
}

void drawO()
{
  unsigned long t = millis();
  for (; millis() - t < 1000;) {
    blinkSegment(segCPin);
    blinkSegment(segDPin);
    blinkSegment(segEPin);
    blinkSegment(segGPin);
  }
}

void drawP()
{
  unsigned long t = millis();
  for (; millis() - t < 1000;) {
    blinkSegment(segAPin);
    blinkSegment(segBPin);
    blinkSegment(segEPin);
    blinkSegment(segFPin);
    blinkSegment(segGPin);
  }
}

void drawT()
{
  unsigned long t = millis();
  for (; millis() - t < 1000;) {
    blinkSegment(segDPin);
    blinkSegment(segEPin);
    blinkSegment(segFPin);
    blinkSegment(segGPin);
  }
}

void drawU()
{
  unsigned long t = millis();
  for (; millis() - t < 1000;) {
    blinkSegment(segCPin);
    blinkSegment(segDPin);
    blinkSegment(segEPin);
  }
}

void drawY()
{
  unsigned long t = millis();
  for (; millis() - t < 1000;) {
    blinkSegment(segBPin);
    blinkSegment(segCPin);
    blinkSegment(segDPin);
    blinkSegment(segFPin);
    blinkSegment(segGPin);
  }
}
